export interface CheckoutCommmitOptions {
    force?: boolean | undefined;
}
declare const checkoutCommit: (pathToRepo: string, hash: string, options?: CheckoutCommmitOptions) => Promise<{
    stdout: string;
    stderr: string;
}>;
export default checkoutCommit;
